# Optimized Trading Configuration (Quick Wins)
#
# Copy these values to your .env file to implement the quick-win strategy
# for improved profitability.
#
# CHANGES FROM DEFAULT:
# - Lower trade sizes ($100-$500) - reduces slippage
# - Lower profit threshold (20 bps) - captures more opportunities
# - Increased slippage buffer - accounts for real execution
# - Lower pool liquidity requirement - more opportunities
#
# EXPECTED IMPROVEMENT:
# - More trades executed (less filtered by simulation)
# - Lower per-trade profit but higher volume
# - Better success rate

# ============================================================================
# QUICK-WIN OPTIMIZED SETTINGS
# ============================================================================

# Trade sizing (SMALLER = less slippage, faster execution)
MIN_TRADE_SIZE_USD=100
MAX_TRADE_SIZE_USD=500

# Profit threshold (LOWER = more opportunities)
# 20 bps = 0.2% minimum profit before fees
# With $500 trade: $1.00 gross profit target
MIN_PROFIT_BPS=20

# Pool liquidity (LOWER = more pools available)
# $1000 minimum still filters out fake/dead pools
MIN_POOL_LIQUIDITY=1000

# Slippage tolerance (HIGHER = more successful executions)
# 150 bps = 1.5% slippage tolerance
SLIPPAGE_TOLERANCE_BPS=150

# ============================================================================
# NETWORK SETTINGS (unchanged)
# ============================================================================
NETWORK=polygon
POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY

# ============================================================================
# EXECUTION MODE
# ============================================================================
# Start with DRY_RUN=true to test the new settings
# Only set to false after confirming simulations show profit
ENABLE_DRY_RUN=true

# ============================================================================
# ADVANCED: PRIVATE TRANSACTION (optional but recommended)
# ============================================================================
# If you have access to a private/MEV-protected RPC:
# PRIVATE_RPC_URL=https://your-private-rpc.example.com
# USE_PRIVATE_TX=true
